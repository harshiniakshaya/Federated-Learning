Project set up:
in root folder, run:
python -m venv venv
venv\Scripts\activate
pip install pandas numpy scikit-learn 

Step 1:
run:
python initial_training/train_initial_model.py
then:
initial_model.pkl will be stored in the root folder
copy the pkl file to hospital_A and hospital_B folders

Step 2:
hospital_A & hospital_B
run:
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
deactivate

Step 3:
hospital_A
uvicorn app:app --reload --port 8001
uvicorn app2:app2 --reload --port 8002

hospital_B
uvicorn app:app --reload --port 8003
uvicorn app2:app2 --reload --port 8004


pip freeze > requirements.txt